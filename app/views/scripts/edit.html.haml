- content_for :title do
  = "#{@script.name} - #{t(:title_edit)}"

%ul.breadcrumb
  %li
    = link_to t(:title_scripts), scripts_path
  %li
    = link_to @script.name, script_path(@script)
  %li.active= t(:title_edit)

%h3= t(:title_edit)

= render 'form'

